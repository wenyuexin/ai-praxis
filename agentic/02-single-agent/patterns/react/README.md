# ReAct

> 适用范围：推理—行动交替式单智能体模式研究
> 阶段状态：调研优先，暂不收敛实现方案

## 一句话定位

本目录用于整理 ReAct 及其衍生模式，关注推理与行动交替如何改善任务执行与工具使用。

## 快速入口

- 结合上层 `../README.md` 理解 ReAct 在 patterns 体系中的位置。
- 重点关注 thought-action-observation 循环、工具使用耦合、失败模式与变体演化。
- 如出现模式定义、论文结论或实现口径冲突，记录到 `../../../temp/conflict.md`。

## 边界说明

- 放在这里：ReAct 模式、衍生变体、thought-action-observation 循环、模式优劣分析。
- 不放在这里：纯规划方法（放 `../../planning/`）、具体工具协议（放 `../../tool-use/`）、某个框架的实现案例（放 `../../../06-frameworks-and-tools/`）。

## 同目录导航

- 如后续需要整体综述，使用 `overview.md`。
- 如需要记录内容缺口，使用 `backlog.md`。
- 如需要学习或建设顺序，使用 `roadmap.md`。
- 如发现目录范围内冲突，使用 `conflict.md`。
